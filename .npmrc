# Flat node_modules uten symlinks. Påkrevd fordi Azure App Service (Kudu
# zipdeploy) ikke bevarer symlinks ved utpakking — pnpm sin standard
# symlink-baserte node_modules gir da knekte moduler (require('next') krasjer).
node-linker=hoisted
